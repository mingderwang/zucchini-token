{
  "name": "zucchini-token",
  "version": "0.0.2",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "prepare": "forge update foundry-rs/forge-std && forge install openzeppelin/openzeppelin-contracts@v5.2.0 --no-git --no-commit -q",
    "forge:test": "forge test -vv"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0"
  }
}
